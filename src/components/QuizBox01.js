export default function QuizBox01({ color }) {
    return (
        <div
            style={{
                width: "100px",
                height: "100px",
                backgroundColor: color,
                marginBottom: "10px"
            }}
        ></div>
    );
}
